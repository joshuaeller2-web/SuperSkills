interface:
  display_name: "Company Agent Runtime"
  short_description: "Role-specific operating methods for Company OS agents"
  default_prompt: "Use Company Agent Runtime for this authorized Company OS assignment, loading only the role's mapped sub-master and required minor method."
policy:
  allow_implicit_invocation: true
