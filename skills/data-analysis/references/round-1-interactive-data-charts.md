# Interactive data charts

Validate types, missing values, date parsing and ordering before plotting. Choose scales and marks from the question. Support resizing with observer/listener cleanup, keyboard-accessible interactions and a readable data alternative. Test empty inputs, changed data, labels, interpretation and resizing.

Measure rendering cost before changing from SVG to Canvas for many marks. Insert untrusted tooltip labels as text, not interpolated HTML. Preserve direction in flow data rather than automatically symmetrizing it. The source has unchecked example and asset-extension inconsistencies; use its method, not copied snippets. D3 is an optional dependency to verify in the project.

## Provenance

Round 1, calesthio/OpenMontage, commit `08e2151fa02de28a5d6a312b3d575692bf147ad7`.

- https://github.com/calesthio/OpenMontage/blob/08e2151fa02de28a5d6a312b3d575692bf147ad7/.agents/skills/d3-viz/SKILL.md

Original scoped synthesis from inspected primary source. Upstream software and runtime dependencies are not installed by this reference. Behavioral effectiveness remains unverified until task trials.
