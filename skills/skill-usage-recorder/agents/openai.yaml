interface:
  display_name: "Skill Usage Recorder"
  short_description: "Records the skills used and why"
  default_prompt: "Record the primary and supporting skills used for this request, with one short reason for each."
policy:
  allow_implicit_invocation: true
